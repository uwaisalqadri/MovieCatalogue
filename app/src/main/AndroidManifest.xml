<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.masuwes.moviecatalogue">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".MovieApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MovieCatalogue">
        <activity android:name=".presentation.detail.tvshow.DetailTvShowActivity" />
        <activity android:name=".presentation.detail.movie.DetailMovieActivity" />
        <activity
            android:name=".presentation.main.MainActivity"
            android:theme="@style/Theme.NoActionBar"
            android:exported="false"/>
        <activity
            android:name=".presentation.main.SplashScreenActivity"
            android:theme="@style/Theme.NoActionBar"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>